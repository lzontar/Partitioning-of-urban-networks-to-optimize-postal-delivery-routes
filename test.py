# import networkx as nx
# import util.lib.helper as help
#
# if __name__ == '__main__':
#     graph = nx.read_pajek(f'data/graphs/temp/pungert-distances.net')
#
#     distanceMatrix = help.distance_matrix(graph)
#     print(help.optimalTraversal(graph, graph.nodes, distanceMatrix, verbose=True))
